{skill_instruction}

只返回 JSON，不要 Markdown 围栏或解释。JSON 必须包含 markdown、scores（naturalness、clarity、conciseness、teaching）和 findings。改写下列 Markdown，使其成为自然、准确的简体中文：

评分字段必须使用 0 到 100 的整数；不要使用 1 到 5 的量表。

{markdown}
