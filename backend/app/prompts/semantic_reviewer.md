只返回 JSON，不要 Markdown 围栏或解释。必须严格使用以下对象结构：

{{"outcomes":{{"facts_sources":"pass","goals_scope":"pass","teaching":"pass","logic_continuity":"pass"}},"findings":[]}}

其中每个 outcome 只能是 pass、warning 或 blocker。审校下列 Markdown 的事实与来源、目标与范围、教学有效性、逻辑连续性：

{markdown}
